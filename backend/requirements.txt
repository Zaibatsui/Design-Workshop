# Production runtime dependencies — only what the backend actually imports.
# The previous list (134 packages) was a snapshot of the preview env's
# pre-installed base image and included numpy / pandas / openai /
# google-genai / litellm / stripe / boto3 / mypy / black / flake8 etc,
# none of which any module under /app/backend imports. Stripping them
# drops the install from ~4 minutes to ~30 seconds and removes the
# Python 3.11+ pins (numpy 2.4.4) that broke builds on 3.10 hosts.

# --- Web framework ---
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic==2.7.4              # 3.10-compatible; same major as preview pin
python-multipart==0.0.9      # required by FastAPI for UploadFile
itsdangerous==2.2.0          # required by Starlette SessionMiddleware

# --- Database ---
motor==3.3.1
pymongo==4.5.0

# --- Auth (Google OAuth via Authlib) ---
Authlib==1.3.2               # 3.10-compatible release line
httpx==0.27.2                # Authlib's preferred async client

# --- HTTP / scraping ---
requests==2.32.3
beautifulsoup4==4.12.3
playwright==1.59.0           # MUST match the base image's bundled chromium

# --- Utilities ---
python-dotenv==1.0.1
